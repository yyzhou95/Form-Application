<!DOCTYPE html>
<html>
<head>
    <title>Web Application</title>
    <link rel="stylesheet" href="/bootstrap/css/bootstrap.css">
    <link rel="stylesheet" href="/stylesheets/main.css">
</head>
<body>
<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="/">Image Gallery</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link active" href="/list">List <span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item">
                <a class="nav-link active" href="/list/new">Add Image</a>
            </li>
        </ul>
        <ul class="nav navbar-nav list-inline">
            <% if (currentUser) { %>
                <li><a href="#" class="btn btn-light"><strong><%= currentUser.username %></strong></a></li>
                <li>
                    <a href="/logout" class="btn btn-light"> Log out </a>
                </li>
            <% }else { %>
                <li>
                    <a href="/login" class="btn btn-light"> Login </a>
                </li>
                <li>
                    <a href="/register" class="btn btn-light"> Sign Up </a>
                </li>
            <% } %>
        </ul>
    </div>
</nav>
<div class="container">
    <% if (error && error.length > 0) { %>
        <div class="alert alert-danger">
            <%= error %>
        </div>
    <% }else if (success && success.length > 0) { %>
        <div class="alert alert-success">
            <%= success %>
        </div>
    <% } else if (requireLogIn && requireLogIn.length > 0) { %>
        <div class="alert alert-warning">
            <%= requireLogIn %>
        </div>
    <% } %>
</div>

